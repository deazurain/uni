/*
 * Control.c
 *
 * Control will be used by the main method to start controlling the tank.
 * It could contain an extra debug mode to test the sensors of a tank so
 * that we don't have to use different programs. If main accepts a 'mode'
 * parameter to choose the control function. 
 * 
 */
/*
#include "Control.h"

// dependencies

#include "Motor.h"
#include "Sensor.h"
#include "LED.h"
#include "Button.h"

// standard
int Control_init() {

  if(Motor_init() == -1) {
    // Motor couldn't initialize
    goto fail;
  }

  if(Sensor_init() == -1) {
    // Sensor couldn't initialize
    goto fail;
  }

  if(LED_init() == -1) {
    // LED couldn't initialize
    goto fail;
  }

  if(Button_init() == -1) {
    // Button couldn't initialize
    goto fail;
  }

  return 0;

  // time for some spaghetti code :@
  fail:

  return -1;
}


int Control_cleanup() {
  // clean up
  return -1; // error
}

int Control_doPledge() {

  while(1) {
    //pledge that thayng'
  }
  
  return -1; // error

}

int Control_doTests() {

  // run tests, print results

  return -1; // error
  
}
*/

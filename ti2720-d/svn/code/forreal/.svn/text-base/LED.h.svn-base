/*
 * LED.h
 *
 * Defines functions to read data from the LEDs
 */

#ifndef LED_H
#define LED_H

// standard
int LED_init();
int LED_cleanup();

// constants
enum {
  LED_DISABLE = 0,
  LED_ENABLE  = 1,
  LED_FLICKER = 2 
};

enum {
  LED_GREEN   = 0,
  LED_ORANGE  = 1 
};

// methods
int LED_setState(int led_id, int state);

#endif
